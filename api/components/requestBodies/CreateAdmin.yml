# TODO: deviseのgemに合わせる

type: object
properties:
  name:
    type: string
    example: "admin管理者"
  email:
    type: string
    example: "admin@example.com"
  password:
    type: string
    example: "password"
  passwordConfirmation:
    type: string
    example: "password"
