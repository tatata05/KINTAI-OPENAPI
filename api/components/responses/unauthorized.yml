description: "Unauthorized"
content:
  application/json:
    schema:
      type: object
      properties:
        error_code:
          type: string
          example: "Unauthorized"
        message:
          type: array
          items:
            example: "ログインしてください"
