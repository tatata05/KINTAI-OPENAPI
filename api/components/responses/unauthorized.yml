description: "Unauthorized"
content:
  application/json:
    schema:
      type: object
      properties:
        errorCode:
          type: string
          example: "Unauthorized"
        message:
          type: array
          items:
            example: "ログインしてください"
