description: "InternalServerError"
content:
  application/json:
    schema:
      type: object
      properties:
        error_code:
          type: string
          example: "InternalServerError"
        messages:
          type: array
          items:
            type: string
            example: "サーバーエラー"
