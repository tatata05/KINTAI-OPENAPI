description: "BadRequest"
content:
  application/json:
    schema:
      type: object
      properties:
        errorCode:
          type: string
          example: "BadRequest"
        message:
          type: array
          items:
            example: "〇〇を入力してください"
