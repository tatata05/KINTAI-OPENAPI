description: "NotFound"
content:
  application/json:
    schema:
      type: object
      properties:
        error_code:
          type: string
          example: "NotFound"
        message:
          type: array
          items:
            example: "対象データが存在しません。"
