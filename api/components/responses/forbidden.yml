description: "Forbidden"
content:
  application/json:
    schema:
      type: object
      properties:
        errorCode:
          type: string
          example: "Forbidden"
        message:
          type: array
          items:
            example: "権限がありません"
